---
phase: monster-project
plan: 02
type: execute
wave: 2
depends_on: ["monster-project-01"]
files_modified: [".planning/monster-repos-todo.json"]
autonomous: true
requirements: ["MON-02"]
must_haves:
  truths:
    - "Todo file exists and contains every repository URL"
  artifacts:
    - path: ".planning/monster-repos-todo.json"
      provides: "List of all repos to process"
  key_links: []
---
<objective>
Create a persistent todo list that enumerates every repository to be imported into the monorepo.
</objective>

<tasks>

<task type="auto">
  <name>Task: Populate master repository todo</name>
  <files>.planning/monster-repos-todo.json</files>
  <action>
    The `todowrite` tool has already created a todo entry whose content lists all repository URLs.
  </action>
  <verify>
    <automated>cat .planning/monster-repos-todo.json</automated>
  </verify>
  <done>Todo JSON is present, parsable, and includes the full list of repos.</done>
</task>

</tasks>

<success_criteria>
The todo list file is readable and contains the expected URLs.
</success_criteria>