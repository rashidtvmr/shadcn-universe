---
phase: monster-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["turbo.json","package.json","pnpm-workspace.yaml","apps/animate-ui"]
autonomous: true
requirements: ["MON-01"]
must_haves:
  truths:
    - "Turborepo scaffold exists"
    - "Animate UI app runs via `pnpm dev`"
  artifacts:
    - path: "turbo.json"
      provides: "Turborepo pipeline config"
    - path: "apps/animate-ui"
      provides: "Base Next.js app"
  key_links:
    - from: "apps/animate-ui"
      to: "turbo.json"
      via: "workspace inclusion"
---
<objective>
Setup a Turborepo monorepo and import the animate‑ui Next.js app as the first package.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Turborepo scaffold</name>
  <files>turbo.json,pnpm-workspace.yaml,package.json</files>
  <action>
    Run `pnpm init` (if needed), add a `turbo.json` with basic pipelines (`dev`, `build`), and configure `pnpm-workspace.yaml` to include `apps/*` and `packages/*`.
  </action>
  <verify>
    <automated>pnpm turbo run build --filter=apps/animate-ui</automated>
  </verify>
  <done>Turborepo build succeeds without errors for the animate‑ui package.</done>
</task>

<task type="auto">
  <name>Task 2: Add animate‑ui as a package</name>
  <files>apps/animate-ui</files>
  <action>
    Copy the existing `animate-ui` directory into `apps/animate-ui`, adjust its `package.json` name to `@monster/animate-ui`, and ensure it references the workspace root.
  </action>
  <verify>
    <automated>cd apps/animate-ui && pnpm dev --port 3000</automated>
  </verify>
  <done>Local dev server launches and the app renders successfully at http://localhost:3000.</done>
</task>

</tasks>

<success_criteria>
All Turborepo commands (`pnpm dev`, `pnpm turbo run build`) complete successfully and the animate‑ui UI is reachable.
</success_criteria>