---
phase: monster-project
plan: 03
type: execute
wave: 3
depends_on: ["monster-project-02"]
files_modified: ["packages/components/batch-01/*"]
autonomous: true
requirements: ["MON-03"]
must_haves:
  truths:
    - "Components from first batch of repos are copied into the monorepo and compile without errors"
  artifacts:
    - path: "packages/components/batch-01"
      provides: "Grouped component modules"
  key_links:
    - from: "packages/components/batch-01"
      to: "apps/animate-ui"
      via: "import paths"
---
<objective>
Import and group components from the first ~100 repositories into the monorepo.
</objective>

<tasks>

<task type="auto">
  <name>Clone and extract components (batch 1)</name>
  <files>packages/components/batch-01</files>
  <action>
    Iterate over the first 100 entries in the todo list (see `.planning/monster-repos-todo.json`). For each repo:
    1. `git clone <url> temp-repo`
    2. Detect a `components/` or `src/` directory containing UI components.
    3. Copy those components into `packages/components/batch-01/<repo-name>/` preserving file structure.
    4. Adjust any import paths to reference the monorepo workspace (e.g., replace relative imports with workspace package imports).
    5. Run `pnpm lint` and `pnpm build` for the copied package to ensure it compiles.
  </action>
  <verify>
    <automated>pnpm turbo run build --filter=packages/components/batch-01</automated>
  </verify>
  <done>All copied components build successfully and are reachable from the animate‑ui app.</done>
</task>

</tasks>

<success_criteria>
Running the monorepo dev server shows no build errors and the imported components can be imported in code.
</success_criteria>